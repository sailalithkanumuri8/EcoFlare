[project]
name = "model-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["onnxruntime>=1.20.1"]


[tool.pyright]
venvPath = "."
venv = ".venv"
